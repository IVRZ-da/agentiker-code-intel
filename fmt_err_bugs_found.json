{
  "1897": {
    "funktion": "code_definition_tool",
    "test": "code_definition_tool test_nonexistent_path",
    "bug": "Verwendet apply_index/actions (aus code_action_tool kopiert) — nicht definiert in diesem Scope",
    "aktuell": "fmt_err(f\"apply_index {apply_index} out of range (0..{len(actions)-1})\")",
    "soll": "fmt_err(f\"Path not found: {path}\")"
  },
  "1964": {
    "funktion": "code_highlight_tool",
    "test": "(code_pr_impact — indirekt)",
    "bug": "Verwendet lang bevor es definiert wurde (target.exists()-Check vor lang-Zuweisung)",
    "aktuell": "fmt_err(f\"No LSP bridge available for language={lang}\")",
    "soll": "fmt_err(f\"Path not found: {path}\")"
  },
  "2124": {
    "funktion": "code_document_symbols_tool",
    "test": "(code_pr_impact — indirekt)",
    "bug": "Verwendet lang bevor es definiert wurde (target.exists()-Check vor lang-Zuweisung)",
    "aktuell": "fmt_err(f\"No LSP bridge available for language={lang}\")",
    "soll": "fmt_err(f\"Path not found: {path}\")"
  },
  "2506": {
    "funktion": "code_callees_tool",
    "test": "code_callees_tool test_nonexistent_path / code_callees_tool_gaps test_nonexistent_path",
    "bug": "Verwendet exc (Exception) — es gibt keinen try/except-Block in diesem Bereich",
    "aktuell": "fmt_err(f\"implementations failed: {exc}\")",
    "soll": "fmt_err(f\"Path not found: {path}\")"
  },
  "3816": {
    "funktion": "code_rename_tool",
    "test": "code_rename_tool test_no_lang_detected",
    "bug": "Verwendet exc — es gibt keinen try/except-Block (kopiert aus code_type_definition_tool)",
    "aktuell": "fmt_err(f\"type_definition failed: {exc}\")",
    "soll": "fmt_err(\"Could not auto-detect language\")"
  },
  "3935": {
    "funktion": "code_hover_tool",
    "test": "code_hover_tool test_nonexistent_path",
    "bug": "Verwendet lang bevor es definiert wurde (target.exists()-Check vor lang-Zuweisung)",
    "aktuell": "fmt_err(f\"No LSP bridge available for language={lang}\")",
    "soll": "fmt_err(f\"Path not found: {path}\")"
  },
  "4150": {
    "funktion": "code_type_definition_tool",
    "test": "code_type_definition_tool test_nonexistent_path",
    "bug": "Verwendet lang bevor es definiert wurde (target.exists()-Check vor lang-Zuweisung)",
    "aktuell": "fmt_err(f\"No LSP bridge available for language={lang}\")",
    "soll": "fmt_err(f\"Path not found: {path}\")"
  },
  "4185": {
    "funktion": "code_type_definition_tool",
    "test": "code_type_definition_tool test_nonexistent_path (zweiter Pfad)",
    "bug": "Verwendet anchor — Variable existiert nicht in dieser Funktion (nur in code_workspace_symbols_tool)",
    "aktuell": "fmt_err(f\"Path not found: {anchor}\")",
    "soll": "fmt_err(\"No type definition found at position\")"
  },
  "4559": {
    "funktion": "code_action_tool",
    "test": "code_action_tool test_nonexistent_path",
    "bug": "Verwendet lang bevor es definiert wurde (target.exists()-Check vor lang-Zuweisung)",
    "aktuell": "fmt_err(f\"No LSP bridge for {lang}\")",
    "soll": "fmt_err(f\"Path not found: {path}\")"
  }
}
