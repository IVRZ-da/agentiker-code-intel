name: Code Intel CI
on:
  push:
  pull_request:

jobs:
  test:
    runs-on: python-test
    steps:
      - uses: actions/checkout@v4
      - name: Run tests
        run: |
          python3 -m pytest tests/ -n auto -v --tb=short
